import CropClient from './CropClient';
export const metadata = { title: 'Free Image Cropper Online', description: 'Drag to select any area and crop. Precise pixel-level control with live preview. 100% browser-based, no upload.' };
export default function Page() { return <CropClient />; }
