import BatchClient from './BatchClient';
export const metadata = { title: 'Free Batch Processor Online', description: 'Process multiple images at once. Convert, resize for social media, and download as ZIP. 100% browser-based, no upload.' };
export default function Page() { return <BatchClient />; }
