import RotateClient from './RotateClient';
export const metadata = { title: 'Free Image Rotator Online', description: 'Rotate images by any angle with quick presets for 90°, 180°, and 270°. 100% browser-based, no upload.' };
export default function Page() { return <RotateClient />; }
